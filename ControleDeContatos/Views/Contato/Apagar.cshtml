@{
    ViewData["Title"] = "Apagar Contato";
}

<div class="text-center">
    <h3>Deseja apagar esse contato (nome do contato) ? A ação não poderá ser desfeita.</h3>
</div>

<div class="text-center pd-10">
    <a class="btn btn-primary" asp-controller="Contato" asp-action="Index">Confirmar</a>
    <a class="btn btn-danger" asp-controller="Contato" asp-action="Index">Cancelar</a>
</div>